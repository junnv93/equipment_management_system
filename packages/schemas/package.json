{
  "name": "@equipment-management/schemas",
  "version": "0.1.0",
  "description": "공유 타입과 스키마 정의",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf dist && tsc -p tsconfig.json --skipLibCheck",
    "test": "jest",
    "clean": "rimraf dist",
    "generate:from-drizzle": "tsx scripts/generate-from-drizzle.ts"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "drizzle-zod": "^0.7.1",
    "jest": "^30.3.0",
    "rimraf": "^5.0.5",
    "ts-jest": "^29.1.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "zod": "workspace:*"
  }
}
