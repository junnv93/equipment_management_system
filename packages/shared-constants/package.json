{
  "name": "@equipment-management/shared-constants",
  "version": "0.1.0",
  "description": "공유 상수 및 권한 정의 - SSOT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf dist && tsc -p tsconfig.json --skipLibCheck",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "rimraf": "^6.1.3",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@equipment-management/schemas": "workspace:*"
  }
}
