/**
 * 페이지네이션 공유 상수 — Backend/Frontend SSOT
 *
 * Canonical source는 `@equipment-management/schemas`다. shared-constants는
 * 기존 Backend/Frontend import surface를 유지하기 위해 재수출만 담당한다.
 */
export {
  PAGE_SIZE_OPTIONS,
  DEFAULT_PAGE_SIZE,
  DASHBOARD_ITEM_LIMIT,
  SELECTOR_PAGE_SIZE,
  DASHBOARD_ACTIVITIES_LIMIT,
  MAX_PAGE_SIZE,
  type PageSizeOption,
} from '@equipment-management/schemas';
