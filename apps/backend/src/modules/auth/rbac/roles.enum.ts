/**
 * 사용자 역할 — schemas re-export 브릿지
 *
 * @deprecated `@equipment-management/schemas`에서 직접 import하세요.
 * ```ts
 * import { UserRoleValues, UserRoleEnum, type UserRole, USER_ROLE_VALUES } from '@equipment-management/schemas';
 * ```
 *
 * SSOT: @equipment-management/schemas
 * 이 파일은 import 경로 호환성을 위해 유지되는 브릿지입니다.
 */
export {
  UserRoleValues,
  UserRoleEnum,
  type UserRole,
  USER_ROLE_VALUES,
} from '@equipment-management/schemas';
