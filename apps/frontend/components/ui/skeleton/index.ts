/**
 * Skeleton library — barrel export
 *
 * Pending placeholder 부품집. 모든 컴포넌트는:
 * - `aria-hidden="true"` 자체 (부모가 `role="status"` 책임)
 * - `motion-safe:animate-pulse motion-reduce:opacity-60` reduced-motion 지원
 * - `bg-primary/10` semantic token (하드코딩 0)
 *
 * @see lib/design-tokens/feedback-pending.ts
 */
export { Skeleton } from './skeleton';
export { SkeletonText, type SkeletonTextProps } from './skeleton-text';
export { SkeletonCard, type SkeletonCardProps } from './skeleton-card';
export { SkeletonTableRow, type SkeletonTableRowProps } from './skeleton-table-row';
export { SkeletonHero, type SkeletonHeroProps } from './skeleton-hero';
export { SkeletonForm, type SkeletonFormProps } from './skeleton-form';
