# QP-18-10 Equipment Import Export — Exec Plan

**Date**: 2026-04-08
**Slug**: qp-18-10-export
**Mode**: 2
**Reference**: .claude/exec-plans/completed/2026-04-07-qp-18-06-export.md

## Context

### Part1/Part2가 무엇인지
`packages/db/src/schema/equipment-imports.ts` line 81 주석에 명시:
> "QP-18-10 양식 매핑: 사용 장소 / 수량 / 반납 상태 (Part1 사용 + Part2 반납)"

- **Part1 (사용 확인)**: 공용 장비 반출 시점 — 장비 정보, 사용기간 시작, 사용 장소, 사용 수량, 신청자 서명
- **Part2 (반납 확인)**: 반납 시점 — 반납 수량, 반납 상태(appearance/operation/abnormality), 이상 상세, 반납일, 서명

양식명: `'공용 장비 사용/반납 확인서'` (form-catalog.ts line 103). QP-18-06의 "반출 → 반입" 2단계 구조와 동일.

### equipment-imports 도메인의 어떤 데이터를 export하는가
- 테이블: `equipmentImports` (단일 레코드, `importId` 파라미터로 조회)
- 조인: `users` 2회 (requester, approver)
- 주요 필드: `equipmentName`, `modelName`, `serialNumber`, `manufacturer`, `classification`, `usagePeriodStart`, `usagePeriodEnd`, `usageLocation`, `quantityOut`, `quantityReturned`, `returnedCondition`(jsonb), `returnedAbnormalDetails`, `reason`, `sourceType`(→ rental: vendorName, internal_shared: ownerDepartment), `approvedAt`, `receivedAt`

## Open Questions (사용자 확인 필요)

- [ ] QP-18-10 DOCX 템플릿이 DB `form_templates` 테이블에 이미 업로드되어 있는가?
      → 없으면 Phase 0 선행: 관리자가 UI에서 QP-18-10 템플릿 파일 업로드 필요
      → `getTemplateBuffer('UL-QP-18-10')` 호출 시 템플릿 미존재면 NotFoundException 반환됨

## 설계 원칙
- 신규 파일 생성 없음. 기존 `form-template-export.service.ts` 수정만
- 서명 헬퍼(`insertDocxSignature`), 날짜 헬퍼(`formatQp1806Date`) 신규 작성 없이 클래스 내 기존 private 메서드 재사용
- 새 import 1건 추가 필요: `equipmentImports`
- 컨트롤러/권한/캐시는 변경 없음 (기존 `GET /api/reports/export/form/:formNumber` 엔드포인트 그대로)

## Phase 1: Catalog 플래그 활성화

**목표**: `isFormImplemented('UL-QP-18-10')`이 true 반환하도록.

- File: `packages/shared-constants/src/form-catalog.ts`
  - WHAT: `'UL-QP-18-10'` 항목 `implemented: false → true`

**검증**: `grep -n "UL-QP-18-10" packages/shared-constants/src/form-catalog.ts -A 7 | grep "implemented: true"` 1건 이상

## Phase 2: exportEquipmentImport 구현

**목표**: `FormTemplateExportService`에 QP-18-10 exporter 추가.

- File: `apps/backend/src/modules/reports/form-template-export.service.ts`
  - WHAT: 파일 상단 imports에 `equipmentImports` 테이블 import 추가
  - WHAT: exporters 맵에 `'UL-QP-18-10': (p, s) => this.exportEquipmentImport(p, s)` 추가
  - WHAT: `private async exportEquipmentImport(params, scope): Promise<ExportResult>` 메서드 추가:
    - `params.importId` 필수 검증 (없으면 BadRequestException, code: `'MISSING_IMPORT_ID'`)
    - `equipmentImports` 테이블에서 1건 조회, 없으면 NotFoundException
    - `scope.site` 적용 — import.site !== scope.site이면 NotFoundException (사이트 우회 방지)
    - `users` 2회 조회: requester(requesterId), approver(approverId, nullable)
    - `formTemplateService.getTemplateBuffer('UL-QP-18-10')` → DocxTemplate 로드
    - Part1 셀 매핑: 업체/소유처(`sourceType`에 따라 `vendorName` or `ownerDepartment`), 장비명, 규격/모델, 제조사, 시리얼, 사용 장소, 사용기간(시작/종료), 사용 수량, 반입 사유, 신청자 서명
    - Part2 셀 매핑: 반납 수량, 반납일(`receivedAt`), 반납 상태(`returnedCondition` jsonb 파싱), 이상 상세(`returnedAbnormalDetails`), 서명
    - 날짜 셀: `this.formatQp1806Date(...)` 재사용
    - 서명 셀: `this.insertDocxSignature(..., '(서명)')` 재사용
    - 반환: `{ buffer, mimeType: DOCX_MIME, filename: 'UL-QP-18-10_공용 장비 사용반납 확인서_YYYY-MM-DD.docx' }`

**검증**:
- `pnpm --filter backend exec tsc --noEmit` 에러 0
- `pnpm --filter backend run build` 성공
- `pnpm --filter backend run test` 기존 통과 유지

## Phase 3: E2E 테스트 추가

**목표**: QP-18-10 export가 200 + DOCX를 반환함을 검증.

- File: `apps/frontend/tests/e2e/workflows/wf-20b-self-inspection-export.spec.ts`
  - WHAT: Step 3 추가 — `createEquipmentImport` helper로 반입 신청 생성 후 `importId`로 export 호출
    - `GET /api/reports/export/form/UL-QP-18-10?importId={importId}` → 200 + `wordprocessingml.document` content-type + body.length > 1000
  - 기존 Step 1/Step 2는 변경 없음

**검증**:
- `pnpm --filter frontend exec tsc --noEmit` 에러 0
- 해당 spec 단독 실행

## Final Verification

- `pnpm tsc --noEmit`
- `pnpm --filter backend run test`
- `git diff --name-status main...HEAD | grep '^A' | grep -v '\.claude/' | wc -l` == 0 (신규 파일 없음)
- 변경 파일 3개 이하 (합리적 범위)

## 의사결정 로그

- 2026-04-08: `formatQp1806Date` 이름 그대로 재사용 — QP-18-10도 동일한 `YYYY . MM . DD .` 포맷이 필요하며, 신규 메서드 생성은 최소 코드 원칙 위반
- 2026-04-08: E2E는 wf-20b Step 3 추가 (새 spec 파일 X) — 기존 equipment-import full cycle(wf-13)과 별개로 export만 빠르게 검증하는 것이 목적
