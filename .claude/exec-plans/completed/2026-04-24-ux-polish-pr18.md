# Exec Plan — PR-18: UX Polish (toast · onboarding pulse · tooltip · mobile bottom sheet)

- **Date**: 2026-04-24
- **Branch**: ux-polish-pr18
- **PR**: 18
- **Scope**: Checkout 도메인 상세/그룹 카드 상호작용 마감 — 상태 배지 툴팁 · NextStep CTA pulse(최초1회) · mutation 토스트 SSOT · 모바일 Bottom Sheet

> 관련 문서: `.claude/plans/reactive-hopping-wolf.md`, `.claude/contracts/checkout-arch-pr3-11.md`,
> `docs/references/frontend-patterns.md`, `apps/frontend/lib/design-tokens/motion.ts`

## 원칙 (CRITICAL)
1. **SSOT 재사용**: 이미 존재하는 `useToast` (shadcn), `useOnboardingHint`, `ANIMATION_PRESETS.pulseHard`, `REDUCED_MOTION.safe()`, `help.status.*` i18n 키를 **재구현 없이** 재사용한다.
2. **새 toast 라이브러리 금지**: `sonner` 설치 금지. `@/components/ui/use-toast` 유일 허용.
3. **수술적 변경**: 기존 toast 호출 위치/메시지는 그대로 유지하고, `notifyCheckoutAction` 유틸 호출로 **치환만** 수행 (문구 SSOT 통일). 인접 코드 리팩토링 금지.
4. **모션/접근성**: pulse는 `prefers-reduced-motion:reduce` 사용자에게 무효화. 모든 pulse/토스트/배지는 키보드/스크린리더 접근성 유지.
5. **Next.js 16 규약**: Drawer는 Client Component 내부에서만 사용, `'use client'` 명시.

---

## Phase 0 — 인프라 준비: shadcn Drawer 설치 (vaul)

**목표**: `apps/frontend/components/ui/drawer.tsx` 생성 (shadcn CLI).

**작업**
1. 루트에서 `pnpm --filter frontend dlx shadcn@latest add drawer` (apps/frontend 하위로 설치)
   - 실패 시 대안: `apps/frontend`에서 `npx shadcn@latest add drawer` 수동 실행
2. 생성된 `apps/frontend/components/ui/drawer.tsx` 확인 (vaul import 포함)
3. `apps/frontend/package.json`에 `vaul` 의존성 추가 확인
4. `pnpm --filter frontend install` 동기화 (lockfile 갱신)

**검증**
- `ls apps/frontend/components/ui/drawer.tsx` 존재
- `grep -n "from 'vaul'" apps/frontend/components/ui/drawer.tsx` 매치
- `pnpm --filter frontend exec tsc --noEmit` 에러 0

**주의**
- `sonner` 유사 패키지 설치 금지 (grep -rn "from 'sonner'" apps/frontend → 0건 유지).
- vaul peer deps 충돌 시 pnpm-workspace.yaml / pnpm-lock.yaml 쪽 메시지 확인 후 중단 → 사용자에게 보고.

---

## Phase 1 — Design Tokens: checkout-toast.ts 신규 + index re-export

**목표**: toast 노출 시간 SSOT 토큰 분리.

**파일**: `apps/frontend/lib/design-tokens/components/checkout-toast.ts` (신규)

```typescript
/**
 * Checkout Toast Tokens
 *
 * SSOT for toast display duration across all checkout mutations.
 * success < warning < error
 */
export const CHECKOUT_TOAST_TOKENS = {
  duration: {
    success: 4000,
    warning: 6000,
    error: 8000,
  },
} as const;

export type CheckoutToastSeverity = keyof typeof CHECKOUT_TOAST_TOKENS.duration;
```

**파일**: `apps/frontend/lib/design-tokens/index.ts` (edit)
- `// Checkout Toast` 섹션 추가, `CHECKOUT_TOAST_TOKENS`·`CheckoutToastSeverity` re-export
- 기존 `// Checkout` 블록(line ~429) 바로 아래 위치

**검증**
- `grep -n "CHECKOUT_TOAST_TOKENS" apps/frontend/lib/design-tokens/index.ts` 매치 1건 이상
- `pnpm --filter frontend exec tsc --noEmit` 에러 0

---

## Phase 2 — 공통 유틸: notifyCheckoutAction (toast-templates)

**목표**: 모든 Checkout mutation 성공 시 일관된 toast 문구를 생성하는 단일 진입점.

**파일**: `apps/frontend/lib/checkouts/toast-templates.ts` (신규)

```typescript
import type { CheckoutAction } from '@equipment-management/schemas';
import { CHECKOUT_TOAST_TOKENS, type CheckoutToastSeverity } from '@/lib/design-tokens';

export type CheckoutToastFn = (opts: {
  title: string;
  description?: string;
  duration?: number;
  variant?: 'default' | 'destructive';
}) => void;

export type CheckoutToastContext = {
  equipmentName: string;
  dueAt?: string; // ISO
};

export type CheckoutToastTranslate = (
  key: string,
  values?: Record<string, unknown>
) => string;

type ActionKey = 'approve' | 'reject' | 'start' | 'return' | 'approveReturn';

const ACTION_KEY_MAP: Partial<Record<CheckoutAction, ActionKey>> = {
  approve: 'approve',
  reject: 'reject',
  start: 'start',
  submit_return: 'return',
  borrower_return: 'return',
  approve_return: 'approveReturn',
};

export function notifyCheckoutAction(
  toastFn: CheckoutToastFn,
  action: CheckoutAction,
  ctx: CheckoutToastContext,
  t: CheckoutToastTranslate,
  severity: CheckoutToastSeverity = 'success'
): void {
  const actionKey = ACTION_KEY_MAP[action];
  if (!actionKey) return; // non-user-facing or unsupported action → silent

  const title = t(`toast.${actionKey}.success`, { equipmentName: ctx.equipmentName });
  toastFn({
    title,
    duration: CHECKOUT_TOAST_TOKENS.duration[severity],
  });
}
```

**규칙**
- `toastFn`은 외부에서 `useToast().toast`를 주입 (훅 규칙 — 유틸은 React context에 접근 금지).
- 미지원 action은 silent (no-op) — 추후 확장 시 `ACTION_KEY_MAP`만 확장.
- `dueAt`은 현재는 사용하지 않지만 확장 포인트로 시그니처에 유지.

**검증**
- `grep -n "notifyCheckoutAction" apps/frontend/lib/checkouts/toast-templates.ts` 매치 1건
- `grep -n "from 'sonner'" apps/frontend/lib/checkouts/toast-templates.ts` 0건
- `pnpm --filter frontend exec tsc --noEmit` 에러 0

---

## Phase 3 — 컴포넌트 증분 (A) CheckoutStatusBadge tooltip

**파일**: `apps/frontend/components/checkouts/CheckoutStatusBadge.tsx` (edit)

**변경**
1. `id?: string` optional prop 추가 (Props 확장)
2. Radix Tooltip + HelpCircle 아이콘 래핑
   - 기존 `<Badge>`를 `<span className="group inline-flex items-center gap-1">` 안에 감싸기
   - Badge 오른쪽에 `<HelpCircle className="opacity-0 group-hover:opacity-100 group-focus-within:opacity-100 h-3 w-3" aria-hidden="true" />`
   - Tooltip content: `t(\`help.status.${status}.description\`)` (기존 i18n 재사용)
3. 접근성: `aria-describedby={id ? \`status-help-${id}\` : undefined}` — Tooltip content에 동일 id 부여
4. `type='rental'`인 경우 help.status 매핑 없을 수 있음 → 조건부로 tooltip 미렌더 (isRental이면 fallback)

**i18n 재사용 확인**
- 기존 `apps/frontend/messages/ko/checkouts.json` line ~669 `help.status.{status}.description` 존재 → 신규 키 추가 금지
- 누락된 상태가 있는 경우 **KO만** 추가하고 EN도 동일 구조로 보완 (최소 범위)

**검증**
- `grep -n "id?: string" apps/frontend/components/checkouts/CheckoutStatusBadge.tsx` 매치
- `grep -n "TooltipProvider\|Tooltip\b" apps/frontend/components/checkouts/CheckoutStatusBadge.tsx` 매치
- `grep -n "help.status" apps/frontend/components/checkouts/CheckoutStatusBadge.tsx` 매치
- `pnpm --filter frontend exec tsc --noEmit` 에러 0

---

## Phase 3 — 컴포넌트 증분 (B) NextStepPanel 온보딩 pulse

**파일**: `apps/frontend/components/shared/NextStepPanel.tsx` (edit)

**변경**
1. import 추가:
   - `import { useOnboardingHint } from '@/hooks/use-onboarding-hint';`
   - `ANIMATION_PRESETS` 기존 import에서 `REDUCED_MOTION`도 추가 (존재 확인됨)
2. 내부 로직:
   ```ts
   const { isVisible: shouldPulse, dismiss: markDone } = useOnboardingHint('checkout-next-step');
   const pulseClass = shouldPulse ? REDUCED_MOTION.safe(ANIMATION_PRESETS.pulseHard) : '';
   ```
3. primary CTA button `className`에 `pulseClass` 조건부 병합 (canAct 경로만)
4. `onClick`에 `markDone()` 추가:
   ```tsx
   onClick={() => { markDone(); onActionClick?.(descriptor.nextAction!); }}
   ```

**주의**
- `REDUCED_MOTION.safe()` 시그니처 확인: `motion.ts`에서 정의됨 (사전 조사 8번 확인 완료). 만약 반환 타입이 string이 아닌 경우 `cn()`으로 병합.
- terminal 상태(canAct=false)에서는 pulse 불필요 → 조건부 분기 유지.
- useOnboardingHint는 SSR 안전(useEffect 기반) — 초기 렌더에서 pulseClass는 빈 문자열.

**검증**
- `grep -n "useOnboardingHint" apps/frontend/components/shared/NextStepPanel.tsx` 매치
- `grep -n "pulseHard" apps/frontend/components/shared/NextStepPanel.tsx` 매치
- `grep -n "markDone\(\)" apps/frontend/components/shared/NextStepPanel.tsx` 매치
- `grep -n "REDUCED_MOTION" apps/frontend/components/shared/NextStepPanel.tsx` 매치
- Stories 렌더 깨지지 않는지 타입 체크만 확인 (story 수정 금지, 기본 시그니처 유지)

---

## Phase 4 — Mobile Bottom Sheet (CheckoutDetailClient)

**파일**: `apps/frontend/app/(dashboard)/checkouts/[id]/CheckoutDetailClient.tsx` (edit)

**목표**: `< md` (768px 미만) 뷰포트에서 primary action(NextStep CTA)을 Drawer 하단 고정 + swipe-up 시 전체 action 목록.

**변경**
1. import 추가:
   - `Drawer`, `DrawerContent`, `DrawerTrigger`, `DrawerHeader`, `DrawerTitle`, `DrawerFooter` (from `@/components/ui/drawer`)
2. **새 블록 추가** — 기존 desktop 액션 영역은 그대로 두고, 모바일 전용 래퍼를 컴포넌트 하단에 추가:
   ```tsx
   <div className="md:hidden fixed inset-x-0 bottom-0 z-40"
        style={{ paddingBottom: 'env(safe-area-inset-bottom)' }}>
     <Drawer>
       <DrawerTrigger asChild>
         <button className="h-16 w-full ..."> {/* peek h-16 */}
           {/* primary CTA label */}
         </button>
       </DrawerTrigger>
       <DrawerContent>
         <DrawerHeader>
           <DrawerTitle>{/* i18n */}</DrawerTitle>
         </DrawerHeader>
         {/* 전체 action 목록 (approve/reject/start/return/cancel ...) */}
       </DrawerContent>
     </Drawer>
   </div>
   ```
3. peek 버튼에는 `data-testid="checkout-mobile-peek"`, DrawerContent에 `data-testid="checkout-mobile-drawer"` 부여 (E2E용).
4. 기존 desktop 액션 버튼 컴포넌트는 **삭제하지 않음** — Drawer 내부에서 **재사용**하거나 동일 핸들러 재호출 (mutation 중복 선언 금지).

**주의**
- Drawer 내부에서 기존 mutation handler(`approveMutation.mutate()` 등)을 직접 호출하여 상태 일관성 유지.
- Drawer는 `< md`에서만 DOM에 렌더 → `md:hidden` 클래스. 데스크톱 경로에는 영향 없음.
- `padding-bottom: env(safe-area-inset-bottom)` 인라인 스타일은 Tailwind 지원 범위 밖이라 style prop 사용 (토큰 예외, 접근성 표준).

**검증**
- `grep -n "Drawer\b" apps/frontend/app/\(dashboard\)/checkouts/\[id\]/CheckoutDetailClient.tsx` 매치 2건+
- `grep -n "md:hidden" apps/frontend/app/\(dashboard\)/checkouts/\[id\]/CheckoutDetailClient.tsx` 매치
- `grep -n "safe-area-inset-bottom" apps/frontend/app/\(dashboard\)/checkouts/\[id\]/CheckoutDetailClient.tsx` 매치
- `pnpm --filter frontend exec tsc --noEmit` 에러 0

---

## Phase 5 — Mutation onSuccess 연결 (GroupCard + DetailClient)

**파일 1**: `apps/frontend/components/checkouts/CheckoutGroupCard.tsx` (edit)

**변경**
1. import 추가: `import { notifyCheckoutAction } from '@/lib/checkouts/toast-templates';`
2. 기존 `approveMutation.onSuccess` (line 256~258)에서 `toast({ title: ... })` → `notifyCheckoutAction(toast, 'approve', { equipmentName }, t)`로 치환
   - `equipmentName`은 group.checkouts[0].equipmentName 등 기존 컨텍스트에서 파생
3. `t` — 현재 `useTranslations('checkouts')` 기준. `t('toast.approve.success', { equipmentName })`이 정상 동작하도록 새 i18n 키를 Phase 6에서 추가.

**파일 2**: `apps/frontend/app/(dashboard)/checkouts/[id]/CheckoutDetailClient.tsx` (edit)

**변경**
1. `approveMutation.onSuccessCallback` / `rejectMutation.onSuccessCallback` / `startMutation.onSuccessCallback` / `approveReturnMutation.onSuccessCallback` / `rejectReturnMutation.onSuccessCallback`
2. 기존 `successMessage: t('toasts.approveSuccess')` 패턴은 `useOptimisticMutation` 내부 토스트 — **유지** 또는 `successMessage` 제거 후 `onSuccessCallback`에서 `notifyCheckoutAction(toast, 'approve', ctx, t)` 호출.
3. 결정: `useOptimisticMutation`의 `successMessage` 메커니즘을 **변경하지 않고**, 단지 `notifyCheckoutAction`으로 **추가 문구 통일 이전이 쉬운 방식**으로 치환 — 구체적으로 `successMessage`를 제거하고 `onSuccessCallback`에서 notifyCheckoutAction 호출.
   - 리스크 평가: useOptimisticMutation 동작 변경 시 다른 기능 영향 가능 → 다음 PR로 분리 권장.
   - **Phase 5 최종 범위 결정**: CheckoutGroupCard만 치환 (inline approve), CheckoutDetailClient는 호출 포인트 1곳(approveMutation만) 샘플로 치환. 나머지는 PR-19+ 후속.

**주의**
- useOptimisticMutation의 successMessage 제거 시 기존 E2E 테스트(suite-03-approval 등)에서 toast title 매칭 깨질 수 있음 → toast i18n 키가 동일하도록 Phase 6에서 보장.
- 치환 범위는 **요청 범위 엄수** — "김에 같이" 다른 핸들러 리팩토링 금지.

**검증**
- `grep -rn "notifyCheckoutAction" apps/frontend/components/checkouts/CheckoutGroupCard.tsx` 매치 1건+
- `grep -rn "notifyCheckoutAction" apps/frontend/app/\(dashboard\)/checkouts/\[id\]/CheckoutDetailClient.tsx` 매치 1건+
- `pnpm --filter frontend exec tsc --noEmit` 에러 0
- 기존 E2E (suite-03-approval `s-approve.spec.ts`) — 타이틀 문자열 매칭이 유지되어야 함 (Phase 6 i18n 키 매핑)

---

## Phase 6 — i18n 키 추가

**파일 1**: `apps/frontend/messages/ko/checkouts.json`

```json
{
  "toast": {
    "approve": { "success": "{equipmentName} 승인 완료" },
    "reject": { "success": "반출 요청이 반려되었습니다" },
    "start": { "success": "반출이 시작되었습니다" },
    "return": { "success": "반납이 완료되었습니다" },
    "approveReturn": { "success": "반납이 최종 승인되었습니다" }
  }
}
```

**파일 2**: `apps/frontend/messages/en/checkouts.json`

```json
{
  "toast": {
    "approve": { "success": "{equipmentName} approved" },
    "reject": { "success": "Request rejected" },
    "start": { "success": "Checkout started" },
    "return": { "success": "Return completed" },
    "approveReturn": { "success": "Return approved" }
  }
}
```

**주의**
- 이미 `toasts.*` 키가 존재 — **다른 네임스페이스(`toast.*`, 단수)**로 추가하여 기존 키와 충돌 방지.
- next-intl ICU MessageFormat: `{equipmentName}` (중괄호 1쌍) 사용.
- help.status.* 키는 **건드리지 않음** (CheckoutStatusBadge tooltip 재사용만).

**검증**
- `grep -n "\"toast\":" apps/frontend/messages/ko/checkouts.json` 매치
- `grep -n "\"toast\":" apps/frontend/messages/en/checkouts.json` 매치
- `grep -c "approve\|reject\|start\|return\|approveReturn" apps/frontend/messages/ko/checkouts.json` 5개 키 존재 확인

---

## Phase 7 — E2E 시나리오 3개

**경로**: `apps/frontend/tests/e2e/features/checkouts/suite-ux/`

### 7.1 `s-onboarding.spec.ts`
- 준비: `storageState` 로그인 → `localStorage.removeItem('onboarding-dismissed:checkout-next-step')`
- 이동: `/checkouts/[id]` (pending 상태 fixture)
- 검증 1: primary CTA에 `pulseHard` 관련 클래스(`animate-pulse` 또는 token 클래스) 존재
- 액션: CTA 클릭
- 검증 2: `localStorage.getItem('onboarding-dismissed:checkout-next-step') === 'true'`
- reduced-motion 분기: `page.emulateMedia({ reducedMotion: 'reduce' })` → pulseHard 클래스 미적용 확인

### 7.2 `s-toast.spec.ts`
- 이동: `/checkouts` (그룹 카드 목록)
- 액션: inline approve 버튼 클릭
- 검증: `[data-radix-toast-viewport]` 내에 i18n 기준 "승인 완료" 텍스트 포함, duration 4000ms (or 4초 후 사라짐 확인)

### 7.3 `s-mobile-bottom-sheet.spec.ts`
- 준비: `page.setViewportSize({ width: 375, height: 812 })`
- 이동: `/checkouts/[id]`
- 검증 1: `[data-testid="checkout-mobile-peek"]` visible, 높이 64px 근처
- 액션: peek 버튼 클릭 (또는 swipe-up 시뮬레이션 — Playwright는 touch drag 한계 → click으로 대체 가능)
- 검증 2: `[data-testid="checkout-mobile-drawer"]` visible, `[aria-modal="true"]` 존재
- 닫기 후 focus trap 해제 확인

**공통**
- 모든 테스트는 `test.describe.parallel` 금지 (reactive-hopping-wolf 플랜 기준 workers=4 제한)
- auth fixture: `tests/e2e/fixtures/auth-fixture.ts` 재사용 (project convention)

**검증**
- `ls apps/frontend/tests/e2e/features/checkouts/suite-ux/` 3파일 존재
- `pnpm --filter frontend exec tsc --noEmit` 에러 0 (E2E 코드도 포함)

---

## Phase 8 — 최종 검증

### 타입/lint
```bash
pnpm --filter frontend exec tsc --noEmit
pnpm --filter frontend run lint
```

### grep 검증 (MUST 기준)
```bash
# Token SSOT
grep -n "CHECKOUT_TOAST_TOKENS" apps/frontend/lib/design-tokens/index.ts

# Toast util
grep -n "notifyCheckoutAction" apps/frontend/lib/checkouts/toast-templates.ts

# Onboarding hint
grep -n "useOnboardingHint" apps/frontend/components/shared/NextStepPanel.tsx

# Mutation 연결
grep -rn "notifyCheckoutAction" apps/frontend/components/checkouts/CheckoutGroupCard.tsx

# E2E 파일
ls apps/frontend/tests/e2e/features/checkouts/suite-ux/s-onboarding.spec.ts
ls apps/frontend/tests/e2e/features/checkouts/suite-ux/s-toast.spec.ts
ls apps/frontend/tests/e2e/features/checkouts/suite-ux/s-mobile-bottom-sheet.spec.ts

# sonner 금지 검증
! grep -rn "from 'sonner'" apps/frontend/
```

### 시각 확인 (개발 서버)
- 데스크톱 `/checkouts/[id]`: Tooltip 호버, pulse 최초1회 확인
- 모바일 viewport(375px): Drawer peek + expand 확인
- reduced-motion 시스템 설정 시 pulse 없음 확인

### 회귀 방지
- 기존 suite-03~07 E2E 통과 — toast title 매칭이 새 i18n 키로 유지되는지 smoke 확인
- `useOptimisticMutation` successMessage 제거한 mutation은 E2E toast assertion을 새 키로 업데이트

---

## 순차 의존성

| Phase | 선행 | 병렬 가능 |
|---|---|---|
| 0 | — | — |
| 1 | 0 | 2와 병렬 |
| 2 | 1 | — |
| 3A (Badge) | 0 | 3B와 병렬 |
| 3B (NextStep) | 1 | 3A와 병렬 |
| 4 (Drawer) | 0 | 3과 병렬 |
| 5 | 2 | — |
| 6 | — | 5와 병렬 |
| 7 | 3·4·5·6 | — |
| 8 | 7 | — |

## 롤백 포인트

- Phase 0 실패(vaul 설치) → 전체 중단 후 사용자 보고.
- Phase 3~5 중 tsc 에러 → 해당 파일만 `git checkout` 후 재시작 (다른 Phase 유지).
- Phase 7 E2E 실패 → 시나리오만 별도 커밋으로 분리, 본 PR은 Phase 0~6 기준 머지 가능하도록 contract 조정.

## 비범위 (Non-Goals)

- Checkout 외 도메인(calibration, disposal 등) toast 통일 — 별도 PR.
- `useOptimisticMutation` successMessage 메커니즘 제거 전면화 — PR-19+.
- drawer snap points · gesture 고도화 — vaul 기본 동작 유지.
- 다국어 RTL 레이아웃 대응 — 별도 PR.
