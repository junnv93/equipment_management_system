# Exec Plan: WF-17~WF-20 E2E 워크플로우 테스트

> **날짜:** 2026-04-05
> **Mode:** 2 (Full)
> **Slug:** wf-e2e-tests

## Phase 1: 공유 인프라 확장

### 1-1. workflow-helpers.ts 헬퍼 추가 (~150줄)

**WF-17용:**
- `setCheckoutOverdue(checkoutId)`: checkout status→overdue, expectedReturnDate→과거일 (DB 직접 설정 또는 API)
- `resetCheckoutOverdue(equipmentId)`: 장비→available 복원

**WF-18용:**
- `rejectCorrection(page, ncId, reason, role)`: /api/non-conformances/:uuid/reject-correction CAS-Aware

**WF-19용:**
- `createIntermediateInspection(page, calibrationId, data, role)`
- `submitIntermediateInspection(page, inspectionId, role)`
- `reviewIntermediateInspection(page, inspectionId, role)`
- `approveIntermediateInspection(page, inspectionId, role)`
- `rejectIntermediateInspection(page, inspectionId, reason, role)`
- `resetIntermediateInspections(calibrationId)`

**WF-20용:**
- `createSelfInspection(page, equipmentId, data, role)`
- `updateSelfInspection(page, inspectionId, data, role)`
- `confirmSelfInspection(page, inspectionId, role)`
- `deleteSelfInspection(page, inspectionId, role)`
- `resetSelfInspections(equipmentId)`

### 1-2. shared-test-data.ts

- WF-17 전용 장비 할당: 다른 WF와 미충돌 장비 선택 (RECEIVER_UIW_W 또는 유사)
- 기존 상수 재사용: CALIB_001 (WF-19), 기존 장비 (WF-20)

**검증:** `pnpm --filter frontend run tsc --noEmit` → 0

## Phase 2: WF-17 — wf-17-checkout-overdue-return.spec.ts (~80줄)

5 steps: 반출 생성→승인→시작→overdue 설정→반입+승인→장비 available

**검증:** `npx playwright test wf-17 --reporter=list` → PASS

## Phase 3: WF-18 — wf-18-nc-correction-rejection.spec.ts (~70줄)

4 steps: NC 생성+조치→반려→재조치→종결

**검증:** `npx playwright test wf-18 --reporter=list` → PASS

## Phase 4: WF-19 — wf-19-intermediate-inspection-3step-approval.spec.ts (~120줄)

9 steps: (정상 6) 생성→상세확인→제출→검토→승인→최종검증 + (반려 3) 생성+제출→반려→상태검증

**검증:** `npx playwright test wf-19 --reporter=list` → PASS

## Phase 5: WF-20 — wf-20-self-inspection-confirmation.spec.ts (~100줄)

7 steps: 생성→상세검증→수정→확인→수정불가→삭제불가→권한테스트

**검증:** `npx playwright test wf-20 --reporter=list` → PASS

## 파일 범위

| 파일 | 액션 |
|------|------|
| `apps/frontend/tests/e2e/workflows/helpers/workflow-helpers.ts` | 수정 (헬퍼 추가) |
| `apps/frontend/tests/e2e/shared/constants/shared-test-data.ts` | 수정 (상수 추가) |
| `apps/frontend/tests/e2e/workflows/wf-17-checkout-overdue-return.spec.ts` | 신규 |
| `apps/frontend/tests/e2e/workflows/wf-18-nc-correction-rejection.spec.ts` | 신규 |
| `apps/frontend/tests/e2e/workflows/wf-19-intermediate-inspection-3step-approval.spec.ts` | 신규 |
| `apps/frontend/tests/e2e/workflows/wf-20-self-inspection-confirmation.spec.ts` | 신규 |
