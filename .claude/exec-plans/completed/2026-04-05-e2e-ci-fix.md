# E2E CI 테스트 87개 실패 수정

## Phase 1: Auth 엔드포인트 수정 (근본 원인)
- [ ] `test-auth.controller.ts`: `/api/auth/test` GET 엔드포인트 추가 (health check)
- [ ] seed-data 테스트 5개 파일: `data.user.role` → `data.user.roles[0]` 또는 `data.user.roles.includes()` 수정

## Phase 2: initialLocation 필드 추가
- [ ] `overdue-auto-nc/permissions.spec.ts`: `createTestEquipment`에 `initialLocation` 추가

## Phase 3: Dashboard 애니메이션 이슈
- [ ] Playwright config에 `prefers-reduced-motion: reduce` 설정 또는 대시보드 테스트에 animation wait 추가

## Phase 4: 개별 테스트 실패 수정
- [ ] calibration-factors 삭제 API 실패 확인
- [ ] calibration-plans 조회/승인 실패 확인
- [ ] checkouts 취소/렌탈 실패 확인

## 검증
- `pnpm --filter backend run tsc --noEmit`
- `pnpm --filter frontend run tsc --noEmit`
