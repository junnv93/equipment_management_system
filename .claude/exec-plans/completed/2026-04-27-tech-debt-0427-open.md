# Exec Plan: tech-debt-0427-open
Date: 2026-04-27
Slug: tech-debt-0427-open
Mode: 2

## 포함 항목

| ID | 항목 | 도메인 |
|----|------|--------|
| P1-1 | use-checkout-next-step-fallback-terminated-from | Frontend hook |
| P1-2 | ar13-self-inspection-approve-weak-cast | Frontend API |
| P1-3 | ar-14-software-validation-approve-comment | Backend DTO + Frontend |
| P1-4 | revocation-window-ms-shared-constants | Backend service → packages |
| P1-5 | not-found-href-ssot | Frontend 4 files |
| P1-6 | create-equipment-import-form-react-formevent | Frontend component |
| P1-7 | reject-reason-stale-i18n | Frontend schema + i18n |
| P1-9 | ar13-dashboard-kpi-self-inspection | Backend controller |
| P1-10 | DESCRIPTOR_TABLE 재생성 스크립트 | packages/schemas |
| P1-11 | qr-label-size-preset-micro-ssot | packages/shared-constants |

## 제외 항목

- **P1-8 ar13-lab-manager-self-inspection**: 도메인 정책 결정(lab_manager 자체점검 승인 권한 여부) 사용자 확인 필요. Tracker 유지.

## Archive (기존 완료 [x] 4건)
- approvals-ui-r2 ar-13-self-inspection-category
- checkout-sprint4-3-to-5 rejection-presets-seed
- checkout-sprint4-3-to-5 stale-time-query-config-presets
- tech-debt-0427-cleanup revoke-approval-workflow-e2e

---

## Phase 0: Archive 이동

### 0.1 tech-debt-tracker.md
완료 [x] 항목 4건 + 처리 완료 항목 제거.

### 0.2 tech-debt-tracker-archive.md
`## 2026-04-27 — tech-debt-0427-open 완료` 섹션 신설 후 이동.

---

## Phase 1-A: packages 수정 (의존성 최하위)

### 1-A-1: packages/shared-constants/src/business-rules.ts
- `APPROVAL_REVOCATION_WINDOW_MS = 300_000` 상수 추가
- JSDoc: "승인 후 철회 가능 시간 윈도우(ms). 현장 운영 기준 5분."
- 위치: `HISTORY_CARD_QUERY_LIMIT` 아래

### 1-A-2: packages/shared-constants/src/index.ts
- `APPROVAL_REVOCATION_WINDOW_MS` re-export 추가

### 1-A-3: packages/shared-constants/src/qr-config.ts (P1-11)
- `LabelSizePreset` type에 `'micro'` 추가
- `LABEL_SIZE_PRESETS.micro: { widthMm: 30, heightMm: 12, qrSizeMm: 8 }` 추가
- `LABEL_SAMPLER_LAYOUT.micro: { rows: 1, cols: 1 }` 추가
- `getSamplerPresetOrder()` return 배열 끝에 `'micro'` 추가

### 1-A-4: packages/schemas/scripts/gen-descriptor-table.ts (신규)
- `buildDescriptorTable()` 실행 후 `descriptor-table.ts`를 인자 없이 덮어쓰는 standalone 스크립트
- tsx로 실행 가능
- 헤더 주석: "AUTO-GENERATED — do not edit. Run `pnpm --filter schemas run gen:descriptor-table`."

### 1-A-5: packages/schemas/package.json
- scripts에 `"gen:descriptor-table": "tsx scripts/gen-descriptor-table.ts"` 추가
- devDependencies에 `"tsx": "^4.19.2"` 추가 (기존 없으면)

---

## Phase 1-B: Backend 수정

### 1-B-1: apps/backend/src/modules/checkouts/checkouts.service.ts:3204
- 로컬 `const REVOCATION_WINDOW_MS = 300_000` 제거
- `APPROVAL_REVOCATION_WINDOW_MS` import 추가
- 비교문 교체

### 1-B-2: apps/backend/src/modules/software-validations/dto/approve-validation.dto.ts (P1-3)
- `approvalComment: z.string().trim().max(VALIDATION_RULES.LONG_TEXT_MAX_LENGTH).optional()` 추가
- `VALIDATION_RULES` import 추가

### 1-B-3: apps/backend/src/modules/software-validations/software-validations.service.ts (P1-3)
- `approve(id, version, approverId, approvalComment?: string)` 시그니처에 4th param 추가
- approvalComment는 audit metadata로 처리 (DB persist 없음 — 수술적 최소 변경)

### 1-B-4: apps/backend/src/modules/software-validations/software-validations.controller.ts (P1-3)
- approve() 메서드에서 `dto.approvalComment` service로 전달

### 1-B-5: apps/backend/src/modules/self-inspections/self-inspections.controller.ts (P1-9)
- approve 메서드 line 222: `@AuditLog({ action: 'update', ... })` → `@AuditLog({ action: 'approve', ... })`
- reject 메서드 line 239: `@AuditLog({ action: 'update', ... })` → `@AuditLog({ action: 'reject', ... })`

---

## Phase 1-C: Frontend API 수정

### 1-C-1: apps/frontend/lib/api/software-api.ts:239 (P1-3)
- `approve(id, version, approvalComment?: string)` 3-arity로 확장
- request body에 approvalComment 포함

### 1-C-2: apps/frontend/lib/api/approvals-api.ts:844~850 (P1-2, P1-3)
- 'self_inspection' approve case weak cast 제거 → 응답 타입 명시
- 'self_inspection' reject case (line 953~960) 동일 패턴 수정
- 'software_validation' approve case comment 전달 추가
- TAB_META.software_validation.commentRequired: false → true
- AR-14 가이드 주석 갱신

---

## Phase 1-D: Frontend Components + Hooks + i18n

### 1-D-1: apps/frontend/hooks/use-checkout-next-step.ts (P1-1)
- `UseCheckoutNextStepInput`에 `terminatedFromStatus?: CheckoutStatus | null` 추가
- 구조분해에 추가
- getNextStep 호출에 terminatedFromStatus 전달
- useMemo deps에 추가

### 1-D-2: apps/frontend/components/equipment-imports/CreateEquipmentImportForm.tsx:113 (P1-6)
- handleSubmit 파라미터 타입 `React.FormEvent` → `React.SyntheticEvent<HTMLFormElement>`

### 1-D-3: apps/frontend/lib/api/approvals-api.ts:213~218 (P1-7)
- RejectReasonSchema `.min(1)` → `.min(REJECTION_MIN_LENGTH)` (이미 정의된 const 사용)
- 주석 교체: "defense in depth — backend 동일 규칙(REJECTION_MIN_LENGTH) SSOT"

### 1-D-4: apps/frontend/messages/ko/approvals.json (P1-7)
- `rejectModal.validation` 및 `bulk.rejectValidation` 메시지 업데이트

### 1-D-5: apps/frontend/messages/en/approvals.json (P1-7)
- ko와 parity 유지

### 1-D-6: 4개 not-found.tsx 파일 (P1-5)
- `href="/"` → `href={FRONTEND_ROUTES.DASHBOARD}` (FRONTEND_ROUTES === '/' SSOT)

---

## Phase 2: Tracker 업데이트

- tech-debt-tracker.md: 처리 항목 [x] 마킹
- tech-debt-tracker-archive.md: 신규 섹션에 이동

---

## Verification

```bash
pnpm --filter @equipment-management/shared-constants build
pnpm --filter @equipment-management/schemas build
pnpm --filter backend run tsc --noEmit
pnpm --filter frontend run tsc --noEmit
pnpm --filter backend run test
```
