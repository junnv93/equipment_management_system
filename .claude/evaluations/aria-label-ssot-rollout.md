# Evaluation: aria-label-ssot-rollout
Date: 2026-04-08 / Iteration: 1

## MUST criteria
| # | Criterion | Verdict | Evidence |
|---|---|---|---|
| 1 | `pnpm --filter frontend exec tsc --noEmit` exit 0 | PASS | No output, exit 0 |
| 2 | `pnpm --filter frontend run test` passes | PASS | 3 suites / 99 tests passed, 0 skipped |
| 3 | verify-i18n: ko/en key symmetry + ICU var match | PASS | `intermediateInspection.actions` keys identical in ko/en; `{date}`/`{name}` vars match across all 3 changed keys (softwareTab.unlinkAriaLabel, calibrationHistorySection.delete/detailAriaLabel) |
| 4 | All *AriaLabel keys contain ≥1 ICU var (date/name) | PASS | submit/review/approve/rejectAriaLabel use `{date}`; unlinkAriaLabel uses `{name}`; delete/detailAriaLabel use `{date}` |
| 5 | grep `name: '수정'` in self-inspect/intermediate/cable/software/calibration specs | PASS | 0 matches |
| 6 | IntermediateInspectionList 4 row actions have contextual aria-label | PASS | submit(L200), review(L215), reject(L227 + L255), approve(L243) all pass `{ date: inspectionDateLabel }` via t() with proper ICU variable |
| 7 | SoftwareTab linkedSoftware unlink has aria-label with SW name | PASS | Line 309 `aria-label={t('softwareTab.unlinkAriaLabel', { name: sw.name })}` |
| 8 | CalibrationHistorySection delete/detail aria-label with `{date}` | PASS | Both pass `fmtDate(item.calibrationDate)`; JSON body now uses `{date}` (not dead ICU) |
| 9 | WF-20 self-inspection regression | PASS | Not in diff scope; no changes to SelfInspectionTab or wf-20 specs |

## SHOULD criteria
| # | Criterion | Verdict | Note |
|---|---|---|---|
| 1 | Cross-component SSOT pattern consistency | PASS | All 4 components use `t('...AriaLabel', { <var> })` contextual pattern |
| 2 | e2e specs use SelfInspectionTab helpers | N/A | No e2e specs modified (Phase 3 conditional, no matching legacy selectors) |
| 3 | review-architecture Critical issues 0 | PASS | Minimal surgical changes, no cross-layer risk |
| 4 | Cable tech-debt-tracker entry | NOT VERIFIED | Out of runtime verification scope |

## Issues found
None. Scope creep check: only the 3 components + 2 equipment.json + 2 calibration.json (exec-plan listed 6 i18n files but SW keys correctly live in equipment.json under softwareTab since SoftwareTab uses `equipment` namespace — this is the right placement, not a deviation). `next-env.d.ts` + `tsconfig.tsbuildinfo` are autogenerated.

Note: Exec-plan mentioned `messages/{ko,en}/software.json` but SoftwareTab.tsx uses `useTranslations('equipment')`, so `softwareTab.unlinkAriaLabel` correctly sits in `equipment.json`. Generator made the right call.

## Final verdict: PASS
